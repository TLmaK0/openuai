name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            platform: linux/amd64
            output: openuai-linux-amd64
            tags: webkit2_41
          - os: ubuntu-24.04-arm
            platform: linux/arm64
            output: openuai-linux-arm64
            tags: webkit2_41
          - os: macos-latest
            platform: darwin/universal
            output: openuai-macos-universal
            tags: ""
          - os: windows-latest
            platform: windows/amd64
            output: openuai-windows-amd64.exe
            tags: ""

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Wails
        run: go install github.com/wailsapp/wails/v2/cmd/wails@latest

      - name: Install Linux deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev

      - name: Build
        run: wails build -platform ${{ matrix.platform }} -o ${{ matrix.output }} ${{ matrix.tags && format('-tags {0}', matrix.tags) || '' }}
        shell: bash

      - name: Package macOS app
        if: runner.os == 'macOS'
        run: |
          cd build/bin
          # Wails produces openuai.app regardless of -o flag
          zip -r ${{ matrix.output }}.zip openuai.app

      - name: Upload artifact (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.output }}.zip
          path: build/bin/${{ matrix.output }}.zip

      - name: Upload artifact
        if: runner.os != 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.output }}
          path: build/bin/${{ matrix.output }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: artifacts/**/*
